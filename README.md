## Hello World project on Ubuntu Server Deployment
- Backend: golang + goFiber
- Install Nginx Server
- DockerFile + DockerCompose: if the server restarts the container should also automatically start.

